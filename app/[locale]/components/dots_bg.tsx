"use client";

import { useEffect, useRef } from "react";

export default function Dots_bg() {
	const bg = useRef<HTMLDivElement>(null);

	useEffect(() => {
        
    }, []);

	return <div ref={bg}></div>;
}
